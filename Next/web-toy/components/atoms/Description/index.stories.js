import React from "react";
import Description from ".";

export default {
  title: "Atoms / Description",
  component: Description
}

const Template = (args) => <Description />

export const Primary = Template.bind({})

export const Secondary = Template.bind({})
