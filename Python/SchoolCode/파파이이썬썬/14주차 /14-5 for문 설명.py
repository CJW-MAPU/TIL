# 반복문 명령:  for문 while문
# 반목문 분류: 정해진 횟수만큼 반복 (for), 정해진 조건에 따라 반복 (while)


#for문 형식
for i in range(5):
    #for ~ in (키워드) i는 임시변수
    #range(j, k, l) = range(0, 5, 1) 이터러블 (반복 제어 객체들, 객체명 변경 가능)
    #j부터 k까지 l씩 증가

    print('HELLO') #실행문, 이터러블의 지정횟수만큼 반복
