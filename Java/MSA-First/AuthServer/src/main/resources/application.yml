server:
  port: 8200

spring:
  application:
    name: auth-resource-service
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/MsaAuth?characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&autoReconnection=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
